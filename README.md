
  # Create as per above

  This is a code bundle for Create as per above. The original project is available at https://www.figma.com/design/ZJDgzU0Y3cIvm3U1PIL4Qx/Create-as-per-above.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  